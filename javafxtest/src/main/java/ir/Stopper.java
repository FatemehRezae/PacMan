package ir;

import javafx.scene.Group;
import javafx.scene.paint.Color;

public class Stopper extends Coin {
    public Stopper(double x, double y, Color color, Group root) {
        super(x, y, color, root);
    }
}
