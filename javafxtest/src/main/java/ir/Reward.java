package ir;

import javafx.scene.Group;
import javafx.scene.paint.Color;

public class Reward extends Coin{
    public Reward(double x, double y, Color color, Group root) {
        super(x, y, color, root);
    }
}
