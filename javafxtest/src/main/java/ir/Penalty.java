package ir;

import javafx.scene.Group;
import javafx.scene.paint.Color;

public class Penalty extends Coin{
    public Penalty(double x, double y, Color color, Group root) {
        super(x, y, color, root);
    }
}
